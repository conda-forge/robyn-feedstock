{% set name = "robyn" %}
{% set version = "0.13.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/robyn-{{ version }}.tar.gz
  sha256: ef82ae7dfef9a96a8f5ab479ec6fc5fd460d3a7671e8713d20db52f2e9700984

build:
  skip: true  # [py<37]
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('rust') }}
    - maturin >=0.12,<0.13

  host:
    - maturin >=0.12,<0.13
    - pip
    - python
    - rust
  run:
    - multiprocess ==0.70.12.2
    - python
    - requests ==2.26.0
    - uvloop ==0.16.0  # [unix]
    - watchdog >=2.1.3

test:
  imports:
    - robyn
  commands:
    - pytest -vvv --capture=tee-sys integration_tests  # [not win]
  source_files:
    - integration_tests
  requires:
    - pytest==6.2.5
    - websockets

about:
  home: https://github.com/sansyrox/robyn
  summary: Async Python backend server with a runtime written in Rust.
  license: BSD-2-Clause
  license_file: LICENSE
  doc_url: https://sansyrox.github.io/robyn/

extra:
  recipe-maintainers:
    - thewchan
